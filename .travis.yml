language: node_js

node_js:
    - "10"
    - "6.0.0"

branches:
    only:
        - master
        - dev

git:
    depth: 3
    quiet: true

before_script:
    - make setup_cover

script:
    - yarn test

after_success:
    - make send_cover

notifications:
    email: false
